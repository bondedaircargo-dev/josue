# Lead Finder & Email Automation — Python dependencies
# Install with: pip install -r requirements.txt

# ── HTTP & scraping ───────────────────────────────────────────────────────────
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.1.0                   # faster HTML parser for BeautifulSoup
urllib3>=2.1.0

# ── Email ─────────────────────────────────────────────────────────────────────
Jinja2>=3.1.3                 # template rendering

# ── Configuration ─────────────────────────────────────────────────────────────
python-dotenv>=1.0.0          # loads .env file

# ── Data handling ─────────────────────────────────────────────────────────────
pandas>=2.2.0                 # optional — for data analysis / filtering

# ── Optional — Google Sheets export ──────────────────────────────────────────
# Uncomment if you set GSHEETS_ENABLED=true
# gspread>=6.0.0
# google-auth>=2.28.0

# ── Optional — SerpAPI (more reliable Google results) ─────────────────────────
# Uncomment if you set SERPAPI_KEY
# google-search-results>=2.4.2

# ── Optional — advanced scraping (JavaScript-rendered pages) ─────────────────
# playwright>=1.42.0          # run: playwright install chromium
